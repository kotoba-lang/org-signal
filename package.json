{
  "name": "kotoba-signal-cljs-test",
  "private": true,
  "description": "Dev-only: cljs test harness for the CLJS sibling of this JVM package. Not published, not required by JVM consumers.",
  "devDependencies": {
    "@noble/curves": "^1.9.0",
    "shadow-cljs": "^2.28.20"
  },
  "scripts": {
    "task": "kbb --backend sci scripts/run-task.cljk",
    "tasks": "kbb --backend sci scripts/run-task.cljk"
  }
}
